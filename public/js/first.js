// Generated by CoffeeScript 1.6.3
(function() {
  var image_stack, socket;

  image_stack = [];

  socket = io.connect();

  socket.on('connect', function() {
    return $('#status').html('connected');
  });

  socket.on('message', function(msg) {
    var image;
    console.log(msg);
    image = new Image();
    image.src = msg.message.data[0].images.low_resolution.url;
    return $('#live').append(image);
  });

}).call(this);
